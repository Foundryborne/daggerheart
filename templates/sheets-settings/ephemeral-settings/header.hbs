<header class='item-sheet-header'>
    <img class='profile' src="{{ephemeral.img}}" data-action="editImage" alt="{{ephemeral.name}}">
    <div class='item-info'>
        <h1 class='item-name'><input name="{{concat updatePath "name"}}" type="text" value="{{ephemeral.name}}" placeholder="{{localize "DAGGERHEART.GENERAL.name"}}" aria-label="{{localize "DAGGERHEART.GENERAL.name"}}"></h1>
    </div>
</header>