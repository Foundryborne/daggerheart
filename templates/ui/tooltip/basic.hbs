<div class="daggerheart dh-style tooltip move">
    <div class="tooltip-title-container">
        <img class="tooltip-image" src="{{img}}" />
        <h2 class="tooltip-title">{{localize name}}</h2>
    </div>
    {{#if tags.length}}
        <div class="tags">
            {{#each tags as |tag|}}
                <div class="tag">
                    <span>{{tag}}</span>
                </div>
            {{/each}}
        </div>
    {{/if}}
    {{#if description}}
        <div class="tooltip-description dh-card-description">{{{localize description}}}</div>
    {{/if}}
    {{#if item.system.duration.type}}
        <div class="tooltip-duration">
            <div class="duration-inner-container">
                <span>{{localize "EFFECT.DURATION.Label"}}:</span>
                <span>{{localize (concat "DAGGERHEART.CONFIG.ActiveEffectDuration." item.system.duration.type )}}</span>
            </div>
        </div>
    {{/if}}
</div>