{{#each items}}
    <div class="sidebar-item-container" data-uuid="{{uuid}}" data-action="{{../action}}">
        <img src="{{img}}" data-item-key="img" class="item-list-img">
        <span data-item-key="name" class="item-list-name">{{name}}</span>
    </div>
{{/each}}